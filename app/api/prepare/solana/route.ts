import {NextResponse} from "next/server";import {getSolanaMint,toSolanaBaseUnits} from "@/lib/solana";
export const dynamic="force-dynamic";
export async function POST(request:Request){const body=await request.json().catch(()=>({}));if(!body.userPublicKey)return NextResponse.json({error:"Solana wallet is required"},{status:400});const input=getSolanaMint(String(body.fromToken||"")),output=getSolanaMint(String(body.toToken||""));if(!input||!output)return NextResponse.json({error:"Unsupported Solana token"},{status:400});const amount=toSolanaBaseUnits(String(body.amount||"0"),input.decimals);const headers:Record<string,string>={"content-type":"application/json",accept:"application/json"};if(process.env.JUPITER_API_KEY)headers["x-api-key"]=process.env.JUPITER_API_KEY;const qp=new URLSearchParams({inputMint:input.address,outputMint:output.address,amount,slippageBps:String(body.slippageBps??50)});const qr=await fetch("https://api.jup.ag/swap/v1/quote?"+qp,{headers,cache:"no-store"});if(!qr.ok)return NextResponse.json({error:"Fresh Jupiter quote unavailable"},{status:502});const quoteResponse=await qr.json();const sr=await fetch("https://api.jup.ag/swap/v1/swap",{method:"POST",headers,body:JSON.stringify({quoteResponse,userPublicKey:body.userPublicKey,dynamicComputeUnitLimit:true,prioritizationFeeLamports:"auto"})});if(!sr.ok)return NextResponse.json({error:"Jupiter transaction preparation failed"},{status:502});const swap=await sr.json();if(!swap.swapTransaction)return NextResponse.json({error:"Jupiter returned no transaction"},{status:502});return NextResponse.json({provider:"Jupiter",swapTransaction:swap.swapTransaction,lastValidBlockHeight:swap.lastValidBlockHeight,prioritizationFeeLamports:swap.prioritizationFeeLamports??null})}