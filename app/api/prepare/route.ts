import {NextResponse} from "next/server";import {getProvider} from "@/lib/providers";import {validateProviderTransaction} from "@/lib/execution/validate";
export const dynamic="force-dynamic";
export async function POST(request:Request){const body=await request.json().catch(()=>({}));const amount=Number(body.amount);if(!Number.isFinite(amount)||amount<=0||!body.provider||!body.userAddress)return NextResponse.json({error:"Invalid preparation request"},{status:400});const provider=getProvider(String(body.provider));if(!provider)return NextResponse.json({error:"Unknown provider"},{status:404});const input={fromChain:body.fromChain,toChain:body.toChain,fromToken:String(body.fromToken),toToken:String(body.toToken),amount:String(body.amount),userAddress:String(body.userAddress)};const quote=await provider.quote(input);if(!quote||!quote.tx)return NextResponse.json({error:"Fresh executable quote unavailable"},{status:409});if(typeof input.fromChain!=="number")return NextResponse.json({error:"EVM preparation requires a numeric source chain"},{status:400});const validation=validateProviderTransaction(quote.tx,input.fromChain);if(!validation.ok)return NextResponse.json({error:"Provider transaction failed validation",details:validation.errors},{status:422});const reviewed=Number(body.reviewedReceive||0),fresh=Number(quote.receive),deteriorationBps=reviewed>0&&fresh<reviewed?((reviewed-fresh)/reviewed)*10000:0;return NextResponse.json({provider:quote.provider,receive:fresh,fee:quote.feeUsd??0,etaSeconds:quote.etaSeconds,expiresAt:quote.expiresAt,approvalTxs:quote.approvalTxs??[],tx:quote.tx,tracking:quote.tracking,deteriorationBps,requiresReconfirm:deteriorationBps>Number(body.maxDeteriorationBps??30)})}